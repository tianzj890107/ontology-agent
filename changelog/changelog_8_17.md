# 20260817 分支变更记录

> 从 2026-08-17 起，今天的 ontology-agent 变更统一记录在本文件中。

## 维护规则

- 今天完成的功能修改、规则调整、问题修复、部署或文档变更，按最终状态同步记录在本文件中。
- 同一功能的多次中间修改合并记录，不记录修改流水账，只保留最终用户可见变化、重要内部契约、主要文件和验证结果。
- 服务器目录：`/home/data/zhangzhen_home/zhangzhen/ontology/ontology-agent`；分支：`20260727`；Agent 端口：`47313`。
- 旧 changelog 保留历史记录，今天的新变更不追加到旧文件。

## 2026-08-17

### 当日变更

- 已创建当天独立变更记录；今天后续完成的修改和部署继续按功能最终状态同步在本文件中。
- 新增独立通用建模服务 `47314`：以 ModelingRun 为边界提供创建运行、上传输入、异步执行、事件查询、文件列表/读取和语义校验接口；每个运行使用隔离的 `input/work/output` 工作区，并通过 `mission-*` 安全别名复用现有建模引擎、Decision/Audit 和 Sandbox 能力，不改变现有 47313 工作台、任务状态和文件。
- 补充独立服务认证、运行索引持久化、路径穿越防护、运行间隔离、部署脚本、API/部署文档及工作区回归测试；接口级健康检查、创建 run、文件写入/列表/读取和越界拒绝验证通过。
- 按 47314 验收反馈完成独立性与可靠性加固：standalone 通过显式关闭旧 Web Task persistence，执行不会读写 47313 的 `.web_tasks.json` 或任务历史；required artifact 缺失/空文件/非法 CSV schema 会在 finalize 失败；集中状态机阻止 ANALYZING/VALIDATING/READY_FOR_EXPORT 期间的非法上传、重复执行和外部校验；`/inputs` 仅允许 input namespace 并拒绝内部状态、路径穿越和 symlink 越界；RunStore 持久化完整事件，并将重启时的处理中运行恢复为带原因的 FAILED；显式未知或空 `requestedArtifacts` 返回参数错误。
- 新增上述隔离、产物门禁、状态竞态、输入信任边界、事件恢复和请求参数回归测试；基础回归通过，`3` 项因 macOS 缺少 bubblewrap 跳过。修复完成前未重新启动或开放服务器 47314，47313 保持健康。
- 继续修复 47314 剩余竞态与 API 原子性：将 source-state 校验与状态转换合并到同一 per-run 锁内，区分 execute 内部 `ANALYZING → VALIDATING` 与外部 validate 的允许来源，外部校验无法插入执行中的 run；创建接口先完整校验 `files`、Base64、路径和 `requestedArtifacts`，畸形请求统一返回 422，且不会创建 run 或孤儿目录；初始化 I/O 失败会回滚半初始化 run。新增确定性并发、HTTP malformed request、内部校验、正常创建和零副作用回归测试；完整 unittest `117` 项通过，`3` 项因 macOS 缺少 bubblewrap 跳过，py_compile、bash -n 和 git diff --check 通过。
- 为 47314 增加数据库建模输入：支持传入与 47313 execution-context 兼容的 `database`/`dataSource` 配置，透传到同一 `Task` 数据库上下文，复用既有 `.db_connection.json`、数据库 helper、连接验证和 AES-GCM fail-closed 解密逻辑；API 响应不返回密码，配置可随 run 索引恢复。新增数据库上下文透传、重启恢复和非法配置零副作用测试；完整 unittest `120` 项通过，`3` 项因 macOS 缺少 bubblewrap 跳过。
- 已将最新 47314 standalone 代码部署到服务器并启动，仅重启/启动 47314，未停止或重启 47313；47314 监听 `0.0.0.0:47314`，远程 `/health` 返回正常，未认证业务接口返回 401，47313 原 PID/端口和健康状态保持不变。服务器 `.env` 已配置 `ONTOLOGY_CRYPTO_SECRET`，standalone API key 权限为 `600`。
- 复用 47313 已验证的 React + Ant Design 前端构建，增加 47314 standalone 模式页面：支持 API Key、历史运行、上传文件、数据库连接配置、正式产物选择、启动建模、状态/事件查看和文件预览；页面请求仅调用 47314 自身 `/api/modeling-runs`，不依赖 47313 API。47314 现在直接提供 `http://172.16.10.34:47314/`，静态页面与资源访问验证通过；仅重启 47314，47313 未重启。前端构建及前端契约/standalone 测试通过。
- 调整 47314 standalone 前端正式输出选择项：保留后端真实文件名和接口契约，仅将七个选项显示为“业务对象、逻辑实体、业务属性、实体关系、业务规则、术语、指标”。重新构建并仅重启 47314，页面与健康检查通过，47313 未重启。
- 将 47314 数据库建模输入调整为与 47313 任务流程一致的分步选择：服务端提供安全数据源目录，前端只选择数据库来源、读取 Schema 和数据表、支持全选，再选择解析要素；不再填写 Host、端口、用户名和密码。服务器已注册 `databaseSourceId=12` 对应 `172.16.5.66:5432/ontology` 的加密连接配置，表清单接口实测返回 `guangfeng` Schema 的 51 张表，创建 run 仅提交数据源 ID 和选中表，密码不返回前端。
- 取消 47314 页面手填长期 API Key：访问页面时由服务端签发 30 分钟 HttpOnly 短期浏览会话，API 仍保留长期 API Key 兼容认证，未携带会话/API Key 的请求继续返回 401。数据库改用 `ontology_agent_ro_47314` 专用账号，仅授予 `guangfeng` Schema 的连接、使用 Schema 和 `SELECT` 权限；连接 helper 同时启用 PostgreSQL `default_transaction_read_only=on`。已实测只读账号可读取 51 张表、写表操作被拒绝，47314 正常，47313 未重启。
- 修复 47314 standalone 页面内容过长时无法滚动到底部的问题：为 standalone 根容器启用独立纵向滚动、保留底部安全间距并允许布局按内容高度扩展，确保“开始任务”按钮在小屏或较长数据表列表下可滚动访问。已重新构建并仅部署/重启 47314，47313 未重启。
- 调整 47314“建模输入”布局：数据库建模/文档建模选择控件后的描述输入改为单行 `Input`，并与选择控件统一使用大尺寸高度，避免多行输入框撑高页面。已重新构建并仅重启 47314，47313 未重启。
- 统一 47314“选择文件”与两个数据库/建模方式下拉控件的交互样式：按钮使用蓝色边框、蓝色文字和手形悬浮指针；下拉控件在默认、选中、悬浮和聚焦状态保持蓝色文字/边框反馈。已重新构建并仅重启 47314，47313 未重启。
- 修复 47314 前端资源部署路径错误：构建后的 JS/CSS 已正确放入服务引用的 `frontend/dist/assets/` 目录，不再被静态路由回退为 HTML；已验证 JS 返回 `text/javascript`、CSS 返回 `text/css`，页面可正常加载。
- 细化 47314 建模输入控件样式：下拉框和文件选择按钮默认保持黑色，悬浮、聚焦或打开选择时显示蓝色边框/文字；建模方式选择与描述输入统一为 40px 高度、16px 字号，与“开始建模”按钮保持一致。
- 按实际页面需求回调控件尺寸：文件选择按钮恢复原有大小；“数据库建模/文档建模”选择框及后方描述输入改为 32px 高度、14px 字号，保持紧凑的单行布局。已重新构建部署 47314。
- 47314 点击“开始建模”后切换为共享 Agent 工作台样式：复用现有思维链/事件流 `EventFeed`、项目文件面板 `FilePanel`、文件预览与刷新交互；事件、运行状态和文件内容仍通过 47314 ModelingRun API 获取，不调用 47313 接口。
- 进一步将 47314 运行态外层切换为共享 Agent 工作台结构：复用 `task-view`、`task-header`、`feed`、运行状态展示和文件面板布局，并将本次建模提示词作为用户消息显示；仅替换数据适配层为 47314 ModelingRun，保持 47313 隔离。
- 为 47314 首页增加 `no-store` 缓存策略，并仅重启 47314，避免浏览器继续使用旧入口 HTML；已验证首页引用最新 Agent 工作台 JS/CSS，静态资源 MIME 正确，47313 未重启。
- 取消 47314“建模要求”必填限制：用户留空时，服务端自动使用当前任务 `mission-input` 中的四份 v0.0.1 规范/模板文件，并结合已选数据表或上传文件直接建模；填写内容时仍按用户要求执行。前端提示同步改为“建模要求可选”，新增空提示回归测试并重新部署 47314。
- 修复 47314 点击“开始建模”后白屏：运行态错误提示使用了 Ant Design `Alert`，但入口遗漏组件导入，现已补齐并增加前端契约回归测试。重新构建、部署 `index-CS8EbUQa.js` 并仅重启 47314；公网首页、JS MIME、短期 Cookie 会话 API 均返回 200，47313 原 PID 与访问状态保持不变。完整 unittest `120` 项通过、`3` 项因 macOS 缺少 bubblewrap 跳过，服务器定向 `21` 项通过，py_compile、bash -n 和 git diff --check 通过。
- 修复 47314 思维链将一次连续思考按流式 token 拆成大量“思考中”节点：standalone 运行态现复用共享工作台的事件归一化逻辑，相邻 thinking 增量合并为一个节点，工具调用后开始的新思考阶段仍独立展示；线上样本 `3007` 条 thinking 增量聚合为 `19` 个实际思考阶段。已热更新 `index-KB0LDFGk.js`，首页、JS MIME 和会话 API 返回 200，本次未重启 47314 或 47313；前端构建、定向 `22` 项及完整 unittest `121` 项通过，`3` 项因 macOS 缺少 bubblewrap 跳过，git diff --check 通过。
- 修复 47314 Agent 运行态高度和底部滚动异常：运行态单独使用自适应 flex 高度，事件流与文件面板填充任务头以下剩余空间，去除固定任务体高度和多余底部留白；建模输入页滚动布局保持不变。重新热部署 `index-CVEDicu5.js`/`index-Cx7sCi6p.css`，47314 首页与资源返回 200、健康检查正常，47313 页面标记、PID 和运行逻辑保持不变；前端构建、定向 `22` 项、完整 unittest `121` 项通过，`3` 项因 macOS 缺少 bubblewrap 跳过，git diff --check 通过。
- 在 47314 左侧“历史运行”上方增加“＋ 新任务”按钮：点击后清空当前运行态、输入内容、上传文件和表选择，返回首页建模表单；历史运行记录仍保留。重新热部署 `index-Inovyi9x.js`/`index-p_0sEHDD.css`，47314 首页和资源返回 200、健康检查正常，47313 PID 与页面标记保持不变；前端构建、定向 `22` 项及完整 unittest `121` 项通过，`3` 项因 macOS 缺少 bubblewrap 跳过，git diff --check 通过。
- 修复 47314“历史运行”运行中标题显示完整 prompt 的问题：确认后端已有 `createdAt`，前端新增统一 `standaloneRunTitle`/`formatRunCreatedAt` 映射；明确短标题优先使用 `title`/`name`，短 prompt 保留原文，默认或过长执行指令显示“本体建模”，无效时间显示“—”。列表、optimistic 创建项和 polling 更新共用同一标题/时间来源，第二行改为本地时区 `YYYY-MM-DD HH:mm`，不再显示 run ID；标题和时间不随 CREATED/ANALYZING/VALIDATING/READY/FAILED 状态跳变。已热部署 `index-C8VNbayE.js`，47314 首页/资源/健康检查通过，47313 PID 与页面标记保持不变；构建、定向 `22` 项及完整 unittest `121` 项通过，`3` 项因 macOS 缺少 bubblewrap 跳过，git diff --check 通过。
- 复用 47313 的 `parseCsv`/`CsvPreview` 实现 47314 CSV 文件阅读器：点击 standalone 文件面板中的 `.csv` 后继续通过 47314 文件内容接口读取，并在 Modal 中显示可滚动表格、表头和行列统计；非 CSV 文件仍使用文本预览。已热部署 `index-D1jQkQAx.js`，47314 首页、JS MIME、CSV 内容接口和健康检查通过，47313 PID 保持不变；构建、定向 `22` 项及完整 unittest `121` 项通过，`3` 项因 macOS 缺少 bubblewrap 跳过，git diff --check 通过。
- 修复 47314 XLSX 预览乱码：standalone 文件打开逻辑对 `.xlsx/.xls/.xlsm` 使用二进制 `arrayBuffer` 和 47313 现有 `XLSX.read`/`SpreadsheetPreview`，不再把 Excel 二进制直接按文本解码；CSV 和普通文本预览保持不变。已完成本地构建、定向 `22` 项测试和 git diff --check；按用户要求停止后续部署/服务操作。
- 统一 47313/47314 的建模完成门禁：`Task.stream_turn` 中的 `max_iterations` 现在只限制单个工具调用窗口，不再允许任务因一轮迭代耗尽而提前结束；只要正式输出、`mission-work` 审计或语义校验未通过，就会把具体阻塞项反馈给同一会话并开启下一轮继续执行，只有门禁通过后才允许结束。新增跨端共用执行器回归测试；本地完整 unittest `122` 项通过、`3` 项因 macOS 缺少 bubblewrap 跳过，定向测试 `24` 项通过，py_compile 和 git diff --check 通过。按要求本轮仅修改本地代码，尚未部署或重启 47313/47314。
- 注册 47314 MetaERP PostgreSQL 数据源选项 `databaseSourceId=0`：使用 `172.16.5.163:54321/ontology` 的服务端加密凭据和 `ontology_dev` Schema；只读验证已成功，数据源列表与表清单接口已返回 30 张表。配置动态加载完成，未重启或影响当前运行任务。
- 修复 47314 运行中思考输出变慢和会话跳回问题：事件改为每条追加到 run 专属 `.events.jsonl`，索引只按批次保存轻量元数据，不再每个 thinking token 重写包含全部历史事件的大索引；前端历史列表只取摘要，运行态通过 `events?since=N` 增量拉取，并用 AbortController、请求代次和当前 `runId` 校验丢弃过期响应，避免切换会话后旧轮询抢回页面。服务端增加单 run 并发门禁，已有 `ANALYZING/VALIDATING` 时新执行返回 `409 ACTIVE_RUN_EXISTS` 和活动 run ID。补充事件恢复、索引瘦身、单任务并发和前端契约测试；本轮仅本地修改，未部署或重启服务。
- 部署前补充失败运行的恢复入口：历史列表中的 `FAILED` run 可正常打开，并在运行工作台显示“继续运行”，复用原 run 的输入、提示词和工作区重新执行；服务端允许 `FAILED → ANALYZING`，同时继续保持全局最多一个 `ANALYZING/VALIDATING` run。新增失败重试回归测试；本地构建和测试通过后部署并重启 47314，未重启 47313。
- 解耦“查看历史会话”和“执行运行”：历史列表所有状态均使用可点击、可键盘触发的会话选择入口；先展示轻量 run 摘要，再异步补齐事件，查看其他会话不会取消、暂停或切换后台正在执行的任务。当前运行的单任务门禁仍只作用于继续/新执行请求，不限制会话查看。
- 进一步修复历史会话点击仍停留在新建页的问题：点击列表项立即用已有摘要进入会话工作台，再异步获取详情和事件；即使详情请求延迟或被旧轮询取消，也不会回退到新建页。
- 复用 47313 的 `Composer` 输入对话框到 47314 历史会话：非 `ANALYZING/VALIDATING` 会话底部显示同样的文本输入、附件入口、模型提示和发送按钮；发送文本/附件可在 `CREATED/INPUT_READY/FAILED` run 上继续执行，`READY_FOR_EXPORT` 仅提示新建任务，不影响后台活动 run。
- 修复 MetaERP PostgreSQL 建模连接方式：`db_connection.py` 连接 PostgreSQL/兼容库时读取 `sourceSchema` 并设置安全的 `search_path`，同时保留只读事务；Agent 明确禁止直接把 `.db_connection.json` 的加密密码当明文使用，必须复用解密后的连接 helper。修复后完成本地/远端定向测试并仅重启 47314，47313 未重启。
- 取消 Agent 服务端按用户每日调用次数、Token 和成本进行阻断的本地额度限制：保留用量与成本统计用于观测，实际模型调用不再因本地每日累计值达到阈值而停止；模型网关自身的限流和计费规则仍然有效。新增额度不阻断回归测试，完整 unittest `126` 项通过、`3` 项因 macOS 缺少 bubblewrap 跳过；已同步部署并重启 47313 与 47314，两个服务首页返回 200，47314 `/health` 正常。
- 调整 47314 Agent 运行态布局：复用 47313 的 Composer 和模型选择器，将输入框固定在左侧会话流底部，始终显示（运行中仅禁用），不再跨列覆盖右侧文件面板；新增模型目录接口并让所选模型随 execute/continue 请求传入独立运行。前端生产构建、前端契约测试、standalone 回归测试和 Python 编译检查通过，已部署并仅重启 47314，47313 未重启。
- 修复“先别建模、只回答当前有多少表”仍启动 Agent 建模的问题：47314 对明确的表数量只读问题直接通过服务端数据源连接查询，返回 Schema 对象总数及物理表/视图拆分，状态回到 `INPUT_READY`，不启动模型线程；数据源表清单同时返回对象类型。新增快捷路径回归测试，定向测试 `26` 项通过，已停止该误启动运行并部署/重启 47314，47313 未重启。
- 按用户意图区分 47314 的执行门禁：显式开始/继续建模或 `intent=execute` 才进入“直到正式输出、审计和语义校验全部完成”的强制门禁；表数量等问题咨询以及 `intent=chat` 只执行问答，不触发正式建模收尾校验，并在完成后恢复为可继续输入状态。复用共享意图识别器，扩展“多少/怎么连接”等问题识别，新增问题咨询与显式执行回归测试；本地完整 unittest `129` 项通过、`3` 项因 macOS 缺少 bubblewrap 跳过，py_compile 和 git diff --check 通过。本轮按要求只部署/重启 47314，47313 不重启。
