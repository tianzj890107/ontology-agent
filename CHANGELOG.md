# 20260727 分支变更记录

> 本文档记录 `20260727` 分支对 Ontology Agent 的功能、接口、页面和部署改动。
> 页面路径以仓库根目录/`open-claude/` 为基准；Agent 服务入口为 `open-claude/oc_codex_server.py`。

## 维护规则

- 每次功能、接口、模型、配置、页面或部署修改后，在对应日期下追加记录，不覆盖历史记录。
- 每条记录至少说明：用户可见变化、涉及页面或接口、主要文件、是否需要重启后端。
- 仅 HTML/CSS/JS 变更：同步源码后刷新浏览器即可；Python、模型、接口、环境变量或依赖变更：需要重启本地和服务器服务。
- 服务器当前目录：`/home/wugefei/ontology/ontology-agent`。
- 当前分支：`20260727`；服务端口：`47313`。
- 真实密钥只能放在未提交的 `.env` 或服务器密钥配置中；`.env.example` 只放配置模板。

## 2026-07-27

### 16. 进入当前任务自动加载文件与系统上下文

- 从带任务 ID 的页面进入 Agent 时，自动打开当前任务项目的文件区，不要求再次上传已经存在于项目中的文件。
- 首次发送消息时，将任务名称、提示词、解析要素、目标输出文件、数据源/文档信息以及当前项目文件清单写入 Agent system prompt，后续对话可直接使用这些上下文。
- 涉及页面/接口：`open-claude/codex_web.html`、`POST /api/tasks/{id}/send`。
- 主要文件：`open-claude/codex_web.html`、`open-claude/oc_codex_server.py`。
- 类型：前端与 Python 后端变更，需要重启后端服务。

### 15. 输出文件预览路径兜底

- `logical_entities.csv`、`entity_relations.csv` 等文件在消息中只有文件名、实际位于任务输出目录时，点击预览会自动在当前项目文件清单中定位真实路径。
- 保留精确路径优先，并增加后缀路径和唯一文件名匹配，避免误打开其他目录下的同名文件。
- 涉及页面：`open-claude/codex_web.html` 文件预览区。
- 类型：HTML/JS 静态资源变更，刷新浏览器即可；无需重启后端。

### 14. 任务信息获取失败提示样式

- 任务信息接口暂时失败时，错误内容改为与任务字段一致的灰色小字辅助提示，不再使用醒目的红色大字。
- 涉及页面：`open-claude/codex_web.html` 的当前任务信息侧栏和详情弹窗。
- 类型：HTML/CSS 静态资源变更，刷新浏览器即可；无需重启后端。

### 6. 可拖拽三栏布局

- 保持原有默认宽度：左侧导航约 264px，文件预览区约占右侧 46%。
- 左侧导航与主内容之间新增拖拽分隔条；打开文件区后，主内容与文件预览区之间也新增拖拽分隔条。
- 限制最小/最大宽度，避免内容区被完全挤压；双击分隔条可恢复默认宽度。
- 页面：`open-claude/codex_web.html`。
- 类型：HTML/CSS/JS 静态资源变更，刷新浏览器即可；服务器已同步。

### 7. 文件预览区默认宽度调整

- 文件预览区默认宽度由约 46% 调整为约 23%，约为原默认宽度的一半。
- 双击预览区分隔条恢复时也使用约 23% 的默认宽度；手动拖拽范围不变。
- 页面：`open-claude/codex_web.html`。
- 类型：HTML/CSS/JS 静态资源变更，刷新浏览器即可；服务器已同步。

### 8. 左侧栏历史任务与当前任务信息

- 历史任务清单改为按钮，默认折叠，点击后才展开历史任务列表。
- 左侧栏下方新增当前任务信息卡；进入整合/建模任务时自动展开，显示任务模式、`taskCode` 和本体库 ID。
- 点击当前任务信息卡仍可打开完整任务上下文弹窗。
- 页面：`open-claude/codex_web.html`。
- 类型：HTML/CSS/JS 静态资源变更，刷新浏览器即可；服务器已同步。

### 9. 自动确认按钮颜色调整

- 自动确认开启状态改为低饱和浅绿色背景与深绿色文字，降低视觉刺激并保持状态辨识度。
- 页面：`open-claude/codex_web.html`。
- 类型：HTML/CSS 静态资源变更，刷新浏览器即可；服务器已同步。

### 10. 当前任务信息弹窗浅色化

- “解析要素”“期望输出文件”“选中数据表”等任务上下文标签改为白色背景、浅蓝边框和深色文字。
- 嵌套任务信息区域保持浅色主题，避免残留黑色标签影响阅读。
- 页面：`open-claude/codex_web.html`。
- 类型：HTML/CSS 静态资源变更，刷新浏览器即可；服务器已同步。

### 11. 任务上下文数组标签浅色化

- 进一步覆盖“校验类型”“期望输出文件”“条目”“解析要素”等数组标签的残留深色样式。
- 统一为白色背景、浅蓝边框和深色文字，并处理嵌套任务信息区域。
- 页面：`open-claude/codex_web.html`。
- 类型：HTML/CSS 静态资源变更，刷新浏览器即可；服务器已同步。

### 12. 当前任务信息紧接历史任务区域

- 当前任务信息卡移动为紧接“历史任务”按钮/列表下方，不再被空白区域推到侧边栏底部。
- 历史任务展开时限制列表高度，当前任务信息仍保持可见并自动展开。
- 当前任务信息卡仍可点击打开完整任务弹窗。
- 页面：`open-claude/codex_web.html`。
- 类型：HTML/CSS 静态资源变更，刷新浏览器即可；服务器已同步。

### 13. 侧边栏自动展开完整任务上下文

- 当前任务信息卡下方自动加载并展开 execution-context 的完整内容。
- 展示本体库 ID、任务编码/名称、模型名称、任务类型、解析要素、期望输出文件、输出路径前缀，以及数据源、选中数据表等嵌套信息。
- 点击当前任务信息卡仍然可以打开原有大弹窗；密码字段继续脱敏。
- 页面：`open-claude/codex_web.html`。
- 类型：HTML/CSS/JS 静态资源变更，刷新浏览器即可；服务器已同步。

### 1. Qwen 模型配置与动态模型目录

- 默认 Provider 改为读取 `LLM_PROVIDER`；当前服务器使用 Qwen，不再要求 Anthropic API Key。
- 支持读取 `QWEN_API_KEY`、`QWEN_BASE_URL`、`QWEN_MODEL`、`QWEN_ENABLE_THINKING` 等配置。
- 自动读取项目 `.env`，Shell 环境变量优先于文件配置。
- 根据 `QWEN_VISION_MODELS` 和 `QWEN_TEXT_MODELS` 动态注册模型，保留原有 Anthropic、OpenAI、GLM、Kimi、DeepSeek 模型。
- Qwen OpenAI 兼容适配器支持图片消息和 Qwen thinking 参数。
- 主要文件：`open-claude/open_claude/config.py`、`open-claude/open_claude/openai_compat.py`、`.env.example`、`.gitignore`。
- 类型：Python、模型和环境配置变更，需要重启后端；已部署到服务器。

### 2. 浅色页面主题

- Agent 工作台从深色主题改为白色/浅色主题。
- 侧栏、任务区、文件预览区、弹窗、代码区、表格和审批卡统一使用浅色样式。
- 页面：`open-claude/codex_web.html`。
- 类型：HTML/CSS 静态资源变更，刷新浏览器即可；服务器已同步。

### 3. 任务执行中的自动确认开关

- 任务开始后，在任务顶部增加“自动确认：开/关”按钮。
- 原首页和任务输入区按钮保留，三个按钮共享同一开关状态。
- 状态保存在浏览器 `localStorage` 的 `oc_auto_approve` 中。
- 执行任务期间打开开关，会立即自动放行当前已挂起的审批卡；关闭后后续危险操作恢复人工确认。
- 主要文件：`open-claude/codex_web.html`。
- 类型：HTML/JS 静态资源变更，刷新浏览器即可；服务器已同步。

### 4. 文件列表按目录折叠

- 文件预览区改为按目录分组显示。
- 项目根目录默认折叠，避免工作区杂项文件一打开占满列表。
- 任务目录、ID 目录等子目录默认展开；目录标题可手动展开/收起。
- 全选、下载、MinIO 上传和文件预览逻辑保持不变。
- 主要文件：`open-claude/codex_web.html`。
- 类型：HTML/JS 静态资源变更，刷新浏览器即可；服务器已同步。

### 5. 20260727 分支与服务器部署

- 创建并使用 Git 分支 `20260727`。
- 服务器已从 `origin/20260727` 拉取代码并运行：`python oc_codex_server.py --host 0.0.0.0 --port 47313`。
- 当前部署版本：`ef8d7b7`；当前服务进程由 `nohup` 持续运行。
- 当前服务器 `.env` 已配置 Qwen，真实密钥未提交到 Git。
- 类型：部署变更；Python/模型配置变更已重启服务器服务。

### 6. 当前本体任务会话自动恢复与 ID 隔离

- 从本体元素生成或整合消歧页面进入时，自动恢复当前 `repositoryId + taskCode` 最近一次任务；浏览器会记住该任务，重新进入时优先打开。
- 历史对话列表只请求并展示当前本体库和任务编码下的会话；“新任务”仍可创建，但会自动归属当前任务 ID，不会混入其他本体任务。
- 无手动选择项目时，后端为当前任务 ID 创建独立的沙箱项目目录，保证文件和对话上下文隔离。
- 涉及页面/接口：`open-claude/codex_web.html`、`POST/GET /api/tasks`。
- 主要文件：`open-claude/codex_web.html`、`open-claude/oc_codex_server.py`。
- 类型：前端与 Python 后端接口变更。
- 是否需要重启后端：是；需刷新页面并重启服务器服务。
- 部署状态：本地已验证，待推送部署。

### 7. 任务信息错误提示弱化

- 任务信息接口失败时，提示改为与普通任务信息一致的灰色小字，降低视觉干扰；错误文本仍保留，便于排查接口问题。
- 主要文件：`open-claude/codex_web.html`。
- 类型：HTML/CSS 静态资源变更。
- 是否需要重启后端：否，刷新页面即可。
- 部署状态：待推送部署。

## 后续记录模板

### N. 功能名称

- 用户可见变化：
- 涉及页面/接口：
- 主要文件：
- 类型：前端静态资源 / Python 后端 / 模型配置 / 接口 / 部署。
- 是否需要重启后端：是/否。
- 部署状态：本地已验证 / 已推送 / 已部署 / 待部署。
