## 张真每日日报生成规范

当用户要求生成“日报”“今日工作总结”“日会纪要”时，按照以下规则执行。

### 1. 信息来源

首先检查能够反映用户当天工作的可靠证据，包括：

* 当天由当前 Git 用户产生的 commit；
* 当天修改、新增或删除的文件；
* 当前工作区中尚未提交的改动；
* 与当天任务有关的代码、配置、部署脚本和文档；
* 用户在本次对话中补充的部署、联调、会议、测试或效果验证情况。

可使用以下信息辅助判断：

* `git config user.name`
* `git config user.email`
* `git log --since="today 00:00" --author="<当前用户>" --stat`
* `git log --since="today 00:00" --author="<当前用户>" --oneline`
* `git status --short`
* `git diff --stat`
* `git diff`
* 必要时查看相关文件内容

不要把其他人的提交算作张真的工作。不要仅根据文件名夸大完成情况。

### 2. 输出目标

输出可以直接发送到团队群里的中文日报，只保留当天最重要的 2～3 项工作。

日报风格必须与团队现有会议纪要一致：

进展：
1、事项描述 @张真(张真)
2、事项描述 @张真(张真)
3、事项描述 @张真(张真)

存在明确风险时，再增加：

PS：
风险：
1、风险描述
措施：对应处理方案

没有明确风险时，不输出“风险”和“PS”。

### 3. 内容选择标准

优先保留以下类型的事项：

1. 已完成并可以验证的功能、部署或联调结果；
2. 对项目主目标有直接贡献的阶段性进展；
3. 已完成的测试、效果验证和问题定位；
4. 会影响排期的阻塞、风险及应对措施。

删除或合并以下内容：

* 安装依赖、阅读代码等常规过程；
* 大量零散的小修改；
* 同一功能下可以合并的多个技术动作；
* 无法说明产出或结果的过程描述；
* 与当天主目标关系不大的细节。

### 4. 表述规则

使用“动作 + 对象 + 结果”的形式，例如：

* 已完成本体平台 Agent 任务会话部署，服务已正常运行；
* 完成驾驶舱智能体本体库动态配置开发并通过功能验证；
* 本体平台 Agent 会话智能消歧与整合开发【80%】；
* 完成指定构建规则 Skills 首轮验证，当前效果未达预期，正在定位规则注入和模型执行问题。

状态词必须准确：

* 代码完成且经过必要验证：使用“已完成”；
* 仅完成编码、尚未验证：使用“完成开发，待测试”；
* 尚未完成：使用“开发中【实际进度】”；
* 仅跑通初步流程：使用“完成初版”或“已跑通初步流程”；
* 效果不符合要求：明确写出“效果未达预期”；
* 不确定完成度时，不自行编造百分比。

### 5. 禁止事项

* 不得编造用户没有完成的工作；
* 不得把阅读、尝试或简单修改写成“已完成系统建设”；
* 不得根据 commit message 直接断言功能已经通过测试；
* 不得输出代码修改建议、分析过程或长篇解释；
* 不得超过 3 条主要进展，除非用户明确要求；
* 不得修改任何代码或文件，只生成日报文本。

### 6. 输出前自检

输出前确认：

* 每一项都能从 Git 证据或用户补充信息中找到依据；
* 每一项都包含明确的对象和结果；
* 2～3 项之间不存在重复；
* 用词符合实际完成状态；
* 最终文本可以直接复制到工作群。
