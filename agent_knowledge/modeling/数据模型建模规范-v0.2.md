# 数据模型建模规范-v0.2：静态 Markdown

## 数据模型建模规范-v0.2.xlsx

> 来源文件：`rules/数据模型建模规范-v0.2.xlsx`
> SHA-256（前12位）：`11d41ac72f98`

### 工作表 1

| 规则类别 |
| --- |
| 识别规则：是指在数据架构设计时，识别数据资产目录元素的过程中，需要遵循的规则，比如识别业务对象、逻辑实体、业务属性需要遵循的规则。 |
| 命名规则：是指在数据架构设计时，识别数据资产目录元素后，对元素命名时需要遵循的的规则，比如业务对象命名时，需要用名词命名。 |
| 定义规则：是指在数据架构设计时，命名元素后，在定义元素自身的属性项、结构、元素关系时，需要遵循的规则，比如逻辑实体需要归属于唯一的业务对象 |

### 工作表 2

| 序号 | 元素类型 | 规则类别 | 规则 | 规则目的/解决问题 | 规则描述 | 规则条目化 | 建模指引 | 评估标准 | 规则正例 | 规则反例 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 主题域分类 | 识别规则 | 与BA对齐 | 解决主题域分类与业务架构脱节、数据视角与业务视角不一致的问题，确保数据模型顶层分类与企业业务流程架构保持对齐，使数据资产目录能被业务人员理解和使用。 | 同BA的流程架构L1对齐 | 1.主题域分类层级必须与BA流程架构L1层级一一对应；<br>2.每个主题域分类必须能映射到一个且仅一个BA L1流程域；<br>3.主题域分类的划分粒度与BA L1保持一致，不得自行增删或合并；<br>4.主题域分类的命名应与BA L1流程域名称保持一致或高度对应。 | 1.获取企业最新版BA流程架构L1清单作为输入；<br>2.将BA L1的每个流程域直接映射为数据模型的主题域分类；<br>3.逐个核对主题域分类与BA L1的对应关系，确保无遗漏、无错位；<br>4.当BA L1发生调整时，同步更新主题域分类。 | 1.可逐条建立主题域分类与BA L1的映射对照表，覆盖率100%为合格；<br>2.存在无法映射到BA L1的主题域分类即为不合规；<br>3.一个主题域分类对应多个BA L1或一个BA L1对应多个主题域分类即为不合规。 | 管理采购 |  |  |
| 2 | 主题域分类 | 命名规则 | 全局唯一 | 解决主题域分类命名重复、歧义导致的数据归属混乱和检索困难问题，确保每个主题域分类在全局范围内具有唯一标识。 | 全局唯一，无歧义 | 1.主题域分类名称在全企业范围内必须唯一；<br>2.不得存在同名但含义不同的主题域分类；<br>3.不得存在名称高度相似、易产生混淆的主题域分类；<br>4.名称变更需进行全局唯一性校验。 | 1.建立主题域分类命名登记台账，新增前先检索已有名称；<br>2.对名称进行全局唯一性校验，包括全称、简称、别名；<br>3.建立命名冲突仲裁机制，由数据治理委员会裁定命名归属；<br>4.定期进行全局命名巡检，清理重复或歧义名称。 | 1.全量检索主题域分类名称，重复率为0%为合格；<br>2.随机抽取10个名称进行模糊匹配，无易混淆名称为合格；<br>3.存在两个及以上同名或近义主题域分类即为不合规。 |  |  |  |
| 3 | 主题域分类 | 识别规则 | 与BA对齐 | 解决主题域分类二级层级与业务流程脱节问题，确保数据模型的中观分类与业务流程架构L2保持一致，支撑业务域级别的数据管理和分析。 | 同BA的流程架构L2对齐 | 1.主题域分类二级层级必须与BA流程架构L2层级对应；<br>2.每个二级分类必须归属于一个且仅一个一级分类（BA L1）；<br>3.二级分类的划分粒度与BA L2保持一致；<br>4.二级分类的业务边界与BA L2流程域边界一致。 | 1.基于BA L2流程架构清单，逐层展开主题域分类；<br>2.确保每个L2流程域都有对应的二级主题域分类；<br>3.核对二级分类与一级分类的从属关系，与BA层级关系保持一致；<br>4.当BA L2调整时，同步更新二级主题域分类。 | 1.二级分类与BA L2的映射覆盖率达到100%为合格；<br>2.二级分类的层级从属关系与BA架构不一致即为不合规；<br>3.存在BA L2无对应数据分类或数据分类无对应BA L2的情况即为不合规。 | 管理采购履行 |  |  |
| 4 | 主题域分组 | 命名规则 | 全局唯一 | 解决主题域分组命名重复导致的数据归类混乱问题，确保主题域分组在全局范围内名称唯一、无歧义。 | 全局唯一，无歧义 | 1.主题域分组名称在全企业范围内必须唯一；<br>2.不同主题域分类下的分组也不得重名；<br>3.分组名称不得与主题域分类名称重复；<br>4.名称变更需进行全局唯一性校验。 | 1.建立主题域分组命名全局登记机制；<br>2.新增分组前进行全库名称检索校验；<br>3.对简称、别名也进行唯一性校验；<br>4.建立命名冲突的升级和仲裁流程。 | 1.全量主题域分组名称无重复为合格；<br>2.存在同名分组（即使分属不同分类）即为不合规；<br>3.分组名称与分类名称重名即为不合规。 |  |  |  |
| 5 | 主题域 | 识别规则 | 与BA对齐 | 解决主题域与具体业务流程脱节问题，确保主题域层级与BA流程架构L3对齐，使数据模型的最细分类粒度与业务可操作的流程节点对应。 | 同BA的流程架构L3对齐 | 1.主题域必须与BA流程架构L3层级对应；<br>2.每个主题域必须归属于一个且仅一个主题域分组（BA L2）；<br>3.主题域的业务边界与BA L3流程节点边界一致；<br>4.主题域的划分粒度应支撑业务流程级别的数据管理。 | 1.以BA L3流程节点为蓝本，识别对应的主题域；<br>2.确保每个L3流程节点都有对应的主题域；<br>3.核对主题域与分组、分类的层级从属关系；<br>4.当业务流程发生L3级调整时，同步更新主题域。 | 1.主题域与BA L3的映射覆盖率达到100%为合格；<br>2.层级从属关系与BA架构不一致即为不合规；<br>3.存在BA L3无对应主题域或主题域无对应BA L3的情况即为不合规。 | 管理采购需求 |  |  |
| 6 | 主题域 | 命名规则 | 全局唯一 | 解决主题域命名重复或歧义导致的数据资产归属不清、检索定位困难问题，确保每个主题域在全局范围内名称唯一。 | 全局唯一，无歧义 | 1.主题域名称在全企业范围内必须唯一；<br>2.不同分组下的主题域也不得重名；<br>3.主题域名称不得与上层分类、分组名称重复；<br>4.名称变更需进行全局唯一性校验。 | 1.建立主题域命名全局登记台账；<br>2.新增主题域前进行全库名称检索；<br>3.对简称、别名、英文缩写也进行唯一性校验；<br>4.建立命名规范评审和冲突仲裁机制。 | 1.全量主题域名称无重复为合格；<br>2.存在同名主题域（即使分属不同分组）即为不合规；<br>3.主题域名称与上层分类/分组名称重名即为不合规。 |  |  |  |
| 7 | 业务对象 | 识别规则 | 企业运作和管理过程中不可缺少的重要人、事、物、地信息 | 解决业务对象识别边界不清、将IT系统视角的技术对象误判为业务对象的问题，确保业务对象从业务本质出发识别，真正反映企业核心业务运作的人事物地。 | 可以基于商业设计、价值流、BI识别业务对象，并和业务能力匹配<br>针对业务对象，通常会建立相应流程、组织和IT进行管理<br>来源于业务流程的表、证、单、书，而非 IT 系统视角。 | 1.业务对象必须是企业运作和管理过程中不可缺少的人、事、物、地信息；<br>2.业务对象的识别应基于商业设计、价值流或BI视角，而非IT系统视角；<br>3.业务对象通常对应业务流程中的表、证、单、书等核心单据；<br>4.业务对象应与业务能力相匹配，有相应的流程、组织和IT系统进行管理；<br>5.如果从IT系统的数据表逆向识别，需要从业务视角识别，排除纯技术的对象（如接口表、日志表对应的实体） | 1.从业务价值链和核心流程入手，识别流程中流转的核心单据和核心实体；<br>2.判断该实体是否为业务运作不可或缺，缺少它业务是否无法运转；<br>3.验证是否有对应的业务流程、组织职责和IT系统支撑其管理；<br>4.排除纯技术视角的对象（如接口表、日志表对应的实体）；<br>5.与业务能力地图进行匹配校验。 | 1.每个业务对象能对应到核心业务流程中的关键单据/实体为合格；<br>2.业务对象无法对应业务流程或业务能力即为不合规； | 在采购管理领域，采购需求作为采购流程的源头单据，承接前端业务需求（如工单、预算、项目），驱动货源确认、订单下达、到货验收等环节，为后续成本核算与财务结算提供依据，是非常重要的单据事项 |  |  |
| 8 | 业务对象 | 识别规则 | 有唯一的身份标识信息 | 解决业务对象实例无法唯一区分、标识不稳定导致的数据混乱问题，确保每个业务对象实例都有全局唯一且不变的身份标识。 | 业务对象有唯一性身份标识信息，能区分业务对象的实例，且标识不变<br>业务对象的其他信息会慢变或者经常变化，但业务对象也不会发生本质变化 | 1.业务对象必须有唯一性身份标识信息，能区分不同实例；<br>2.身份标识在业务对象的生命周期内保持不变；<br>3.业务对象的其他属性可以变化，但标识本身不应变化；<br>4.该标识的唯一性范围为全局唯一，而非仅在父对象下局部唯一。 | 1.为每个业务对象定义唯一标识符（如编号、编码）；<br>2.确保标识符的生成规则能保证全局唯一性；<br>3.验证标识符在实例生命周期内不会被修改；<br>4.区分业务对象与其子实体/行项，子实体通常不具备独立全局标识；<br>5.避免使用可变更的名称、描述等作为唯一标识。<br>6.主键为单一业务编号（如 XX 编号/单号/代码）→ 支持；主键为"父键 + 行号/序号"的“复联合主键”，或纯技术自增 ID 且无业务编号 → 不支持（该表是从属逻辑实体，如采购需求行） | 1.每个业务对象都有明确定义的唯一标识符为合格；<br>2.标识符存在重复或可变更即为不合规；<br>3.将子实体/行项误识别为业务对象（无独立全局标识）即为不合规。 | 每一次采购需求单内容都不相同，因此采购需求有采购需求编号唯一标识 | 将采购需求行作为业务对象，是错误的，因为采购需求行没有独立的身份标识，采购需求行号可以基于每个采购需求编号下，重新编码行号（不是全局唯一）。 | 如果针对某次特定业务活动产生的报告，报告本身（报告头）可以作为业务对象（比如某次风险评估报告、某次财务年报，有唯一编号，本质上是一种事物数据） |
| 9 | 业务对象 | 识别规则 | 相对独立并有一组实体描述 | 解决业务对象与逻辑实体边界混淆、将依赖型子实体误判为业务对象的问题，确保业务对象具有独立存在性，并有一组逻辑实体共同描述其特征。 | 业务对象可独立存在，可获取，传输，使用，并发挥价值，而逻辑数据实体依赖于业务对象<br>业务对象有一组描述自己某方面特征的逻辑实体；以主逻辑实体为核心，其他逻辑实体是他的外键子实体、或者外键子实体的外键子实体，往下递归。 | 1.业务对象可独立存在、可获取、可传输、可使用并发挥价值；<br>2.业务对象有一组描述自身各方面特征的逻辑实体；<br>3.以主逻辑实体为核心，其他逻辑实体通过外键关系递归关联，结构上表现为：“1 个主逻辑实体 + 其外键子实体 + 子实体的外键子实体（递归向下）” | 1.判断该逻辑实体是否能脱离其他实体独立存在和管理；<br>2.识别描述该业务对象不同方面特征的逻辑实体集合；<br>3.确定主逻辑实体作为业务对象的核心代表；<br>4.梳理其他逻辑实体与主逻辑实体的外键关联关系；与主逻辑实体“同主键的 1:1 扩展表/垂直拆分表/历史归档表/分表”→ 合并进同一业务对象，不新增对象。纯多对多“关联表/中间表” → 归为关系实体，不独立成对象。<br>5.验证移除父实体后，该实体是否仍有独立意义。 | 1.业务对象能独立存在且有一组逻辑实体描述为合格；<br>2.将依赖型子实体（如行项、明细）误判为业务对象即为不合规；<br>3.业务对象没有对应的主逻辑实体即为不合规。 | 采购需求与供应商、采购订单等业务对象，相对独立，由采购需求行、采购需求行等一组逻辑实体描述。 | 将采购需求行作为业务对象，是错误的，因为采购需求行不能独立存在，其依赖于采购需求头。 | 关键识别原则 |
| 10 | 业务对象 | 识别规则 | 有生命周期和状态变化 | 解决业务对象与基础数据、观测数据边界不清的问题，通过生命周期和状态变化特征区分真正的业务对象与静态参考数据、不可变观测数据。 | 业务对象有生命周期，有状态变化。<br>不同生命周期状态的人事物地，识别为不同的业务对象 | 1.业务对象必须有生命周期，存在状态变化；<br>2.如果人事地物的生命周期状态的划分类型（枚举值）不一样，应识别为不同的业务对象，比如销售合同和销售订单的生命周期划分类型不一样，需要识别为不同业务对象。<br>3.基础数据（码值、分类）和观测数据通常无状态变化，不属于业务对象。 | 1.梳理业务对象的生命周期阶段和状态流转图；<br>2.验证实例是否会经历从创建到终结的完整生命周期；<br>3.区分业务对象与基础数据：基础数据码值有限、无业务行为、仅作分类参考；<br>4.区分业务对象与观测数据：观测数据不可修改、无状态变化；<br>5.同一类事物在不同生命周期阶段若本质不同，应拆分为不同业务对象。 | 1.每个业务对象都有明确定义的生命周期和状态集为合格；<br>2.将基础数据（码值表、分类字典）误识别为业务对象即为不合规；<br>3.将观测数据（不可变的记录型数据）误识别为业务对象即为不合规。 | 采购需求有生命周期状态：草稿、审核中、已审批、已驳回、已退回、已作废 | 如果是基础数据（如果要变更，基本上是新加码值、清洗原有数据）<br>观测数据无状态变化（观测数据不会被修改） | 关键识别原则 |
| 11 | 业务对象 | 识别规则 | 责任主体可确权 | 解决业务对象识别的领域范围扩大化问题，防止将不属于本领域的业务对象误识，导致归属不清、无人负责导致的数据质量无人管控问题，确保每个业务对象都有明确的业务领域归属和数据Owner。 | 业务对象有明确的归属领域，在该业务领域进行全生命周期的管理；并且有明确的业务部门作为数据的Owner，履行管理者职责。 | 1.业务对象必须有明确的归属业务领域；<br>2.在该业务领域内进行全生命周期管理；<br>3.必须有明确的业务部门作为数据Owner；<br>4.Owner部门负责履行数据管理职责（标准制定、质量管控、安全管理等）；<br>5.Owner部门应具备该业务对象全生命周期的管理能力。 | 1.确认识别的每个业务对象都属于当前业务领域；<br>2.识别的业务对象，有负责该业务对象全生命周期管理的业务部门；<br>3.若本领域只是"引用/消费"该业务对象（仅存外键或冗余快照字段），则不在本领域识别为业务对象（如人力领域不得把"身份证"识别为业务对象 | 1.每个业务对象都有明确的归属领域和数据Owner为合格；<br>2.业务对象无明确Owner部门即为不合规；<br>3.Owner部门不具备该业务对象全生命周期管理能力即为不合规。 | 采购订单归属于采购管理领域，采购部门是采购订单全生命周期的管理这者，是数据的Owner | 比如人力资源管理部门，将“身份证”识别为业务对象是错误的，因为人力资源管理部门不是公安部门，不负责身份证的制证、发证、换证、注销等全生命周期管理。 |  |
| 12 | 业务对象 | 识别规则 | 可实例化 | 解决将分类标签、报表指标等不可实例化的概念误判为业务对象的问题，确保业务对象是可产生业务行为、有真实实例的业务实体。 | 业务对象的实例可以发生业务行为<br>业务对象有具体实例存在，通常需明确owner、定义架构、标准、度量监控，才能有效管理<br>业务对象的实例集合是不可提前预知、不限定数量的 | 1.业务对象的实例可以发生业务行为；<br>2.业务对象有具体实例存在，实例集合不可提前预知、数量不限；<br>3.业务对象通常需明确Owner、定义架构、标准、度量监控才能有效管理；<br>4.基础数据（分类/标签）无业务行为，码值数量有限且可预置，不属于业务对象；<br>5.报告报表数据无法实例化，不属于业务对象。 | 1.判断该概念是否有真实的业务实例，且实例数量不可预置（基础数据是预设、数量有限）；<br>2.验证实例是否能执行业务行为；<br>3.区分业务对象与基础数据：基础数据是分类标签，码值有限且固定；<br>4.区分业务对象与报表数据：报表是数据的呈现形式，不可实例化；<br>5.检查是否需要配套的管理流程和标准来管控实例。 | 1.业务对象有不限数量的实例且能执行业务行为为合格；<br>2.将基础数据分类/码值误识别为业务对象即为不合规；<br>3.将报表/统计数据误识别为业务对象即为不合规。 | 采购需求有很多次需求，次数无法预置、不限；采购需求有相应的管理流程；<br>采购需要有相应的行为：创建、审批、退回、取消等业务行为。 | 基础数据是分类/标签,无业务行为，比如采购需求类型；<br>基础数据码值数量是提前确定、有限的<br>如果是报告报表数据，无法实例化 |  |
| 13 | 业务对象 | 命名规则 | 名称唯一 | 解决业务对象命名重复导致的同名异义、异名同义问题，确保每个业务对象在数据模型中具有唯一的名称标识。 | 名称在数据模型中具有唯一性 | 1.业务对象名称在整个数据模型中必须唯一；<br>2.同名必统一：相同名称的业务对象必须是同一事物；<br>3.不同业务对象必须使用不同名称（异名异义）；<br>4.名称变更需进行全局唯一性校验；<br>5.跨领域业务对象命名也需全局唯一。 | 1..新增业务对象前进行全库名称检索校验；<br>2.对简称、别名、英文名称也进行唯一性校验；<br>3.当不同领域出现同名业务对象时，判断是否为同一事物，是则合并，否则分别命名； | 1.全量业务对象名称无重复为合格；<br>2.存在同名但含义不同的业务对象即为不合规；<br>3.存在同一事物多种名称（异名同义）即为不合规。 | 业务对象“采购需求”，名称企业内唯一 | 假设存在（直接采购需求、间接采购需求、服务采购需求等业务对象，他们下面的逻辑实体包含的业务属性也不一样，则需要拆分为三个业务对象，且名称不能相同——异名异义） |  |
| 14 | 业务对象 | 命名规则 | 名词命名 | 解决业务对象命名不规范、使用动词或虚词导致语义模糊的问题，确保业务对象名称使用规范的名词形式，清晰表达事物本质。 | 业务对象名称原则上必须是名词，原则上不使用介词/连词之类的虚词 ：在，被，的等均应去除，中文名称中尽量避免使用英文，禁止使用符号，开始和末尾原则上禁止使用数字 | 1.业务对象名称原则上必须是名词；<br>2.不使用介词、连词等虚词（在、被、的等均应去除）；<br>3.中文名称中尽量避免使用英文；<br>4.禁止使用符号；<br>5.名称开始和末尾原则上禁止使用数字。 | 1.使用名词形式命名业务对象，表达"是什么"而非"做什么"；<br>2.去除名称中的虚词、助词、介词等冗余成分；<br>3.优先使用中文命名，确需英文时使用规范术语；<br>4.名称中不包含特殊符号、标点等；<br>5.不以数字开头或结尾（除非有明确业务含义且符合规范）。 | 1.业务对象名称均为纯名词形式为合格；<br>2.名称中包含动词、虚词即为不合规；<br>3.名称中包含符号、以数字开头或结尾即为不合规。 | 采购需求 | 采购需求申请 |  |
| 15 | 业务对象 | 命名规则 | 符合行规 | 解决业务对象命名过于个性化、与行业通用术语脱节导致的沟通成本高、理解歧义问题，确保命名符合企业和行业通用习惯。 | 名称能够完整准确表述业务含义，符合企业内、行业内的通用习惯和规范 | 1.名称能够完整准确表述业务含义；<br>2.符合企业内部的通用习惯和规范；<br>3.符合所在行业的通用习惯和规范；<br>4.优先使用行业标准术语；<br>5.避免自造词、口语化表达。 | 1.参考行业标准、国家标准中的术语定义；<br>2.参考同行业标杆企业的数据模型命名；<br>3.与企业内部业务术语库对齐；<br>4.组织业务专家评审命名的行业符合性；<br>5.对自造词进行严格审核，确有必要时才使用并配套定义说明。 | 1.业务对象命名与行业通用术语一致或高度对应为合格；<br>2.使用自造词且无充分理由即为不合规；<br>3.命名口语化、非规范化即为不合规。 | 采购需求 | 采需单 |  |
| 16 | 业务对象 | 定义规则 | 编码唯一 | 解决业务对象编码不统一、各领域编码规则不一致导致的系统集成困难、数据交换障碍问题，确保编码全局唯一且规范统一。 | 业务对象元素的编码，在企业内唯一，并且遵循相同的编码规范 | 1.业务对象元素的编码在企业内必须唯一；<br>2.所有业务对象遵循相同的编码规范；<br>3.编码应包含可识别的分类信息（如领域前缀）；<br>4.编码应有统一的长度和格式规范；<br>5.编码变更需遵循统一的变更流程。 | 1.制定企业统一的业务对象编码规范（前缀+领域+序列号等）；<br>2.为每个业务对象分配符合规范的唯一编码；<br>3.建立编码分配登记台账，防止重码；<br>4.编码中体现业务领域、对象类型等分类信息；<br>5.定期进行编码合规性巡检。 | 1.所有业务对象编码全局唯一且遵循统一规范为合格；<br>2.存在重复编码即为不合规；<br>3.不同领域编码规则不统一即为不合规。 | BOPUR001（编码规范由企业自行设计，此处只是给示例BO+领域缩写+3位序列号） | 不同的业务领域，编码规范不一致，有是“BO”开头，有的是序列号，有的是“YWDX”开头，五花八门 |  |
| 17 | 业务对象 | 定义规则 | 描述内容完整 | 解决业务对象定义模糊、描述不完整导致的理解不一致、边界不清问题，确保每个业务对象都有完整、清晰的定义说明。 | 业务对象应该有明确的描述，包括目的（为什么）、定义（是什么）和范围（含哪些），其中业务对象范围不应局限于某类产品 | 1.业务对象必须有明确的描述；<br>2.描述必须包含目的（为什么）：解释该业务对象为何对业务重要；<br>3.描述必须包含定义（是什么）：清晰说明业务对象的本质含义；<br>4.描述必须包含范围（含哪些）：明确业务对象涵盖的业务场景和内容；<br>5.描述应具有业务语义，而非技术描述，慎用“记录XXX”这种技术语言描述。 | 1.按"目的-定义-范围"三段式结构编写业务对象描述；<br>2.目的部分回答"为什么这个业务对象对业务重要"；<br>3.定义部分回答"这个业务对象是什么"；<br>4.范围部分回答"包含哪些业务场景、不包含哪些"； | 1.业务对象描述包含目的、定义、范围三要素为合格；<br>2.描述过于宽泛、无实质内容即为不合规； | 采购需求是企业对物资 / 服务的采购申请与业务承诺，是采购交易处理过程中的单据记录，包含待采购物料 / 服务明细清单、需求数量、交付时间、成本归属，实现企业内部需求部门与采购部门之间的沟通，是业务部门对企业待采物资 / 服务的一种请求，驱动采购计划与订单下达，管理需求履行状态，实现企业对采购活动的计划性控制，使企业的采购活动、生产活动、库存活动处于有序、流畅、高效的状态，支撑成本归集与费用管控。采购需求涉及的业务场景包括生产性需求与非生产性需求，对于生产性需求，采购需求承接生产计划与 MRP 运算结果，驱动物料齐套与供应保障；对于非生产性需求，采购需求承接部门预算与项目计划，驱动服务采购与费用支出管控。采购需求作为采购流程的源头单据，承接前端业务需求（如工单、预算、项目），驱动货源确认、订单下达、到货验收等环节，为后续成本核算与财务结算提供依据。<br>采购需求包括直接采购需求、间接采购需求、服务采购需求。 | 采购需求为了实现采购需求单管理，记录采购的内容和事项（太宽泛、等于什么都没有说） |  |
| 18 | 逻辑实体 | 识别规则 | 一组属性集合 | 解决逻辑实体划分粒度不当、属性随意拼凑导致的语义不统一问题，确保每个逻辑实体是一组紧密相关、有独立完整业务语义的属性集合。 | 描述业务对象不同业务特征的密切相关的一组属性集合，有独立完整业务语义，可以设计为一个逻辑数据实体。<br>1、属性围绕同一个业务概念；<br>2、不可随意拆分；<br>3、不会出现 “部分属性有值、另一部分永远为空”；<br>4、业务上作为一个整体被认知。 | 1.逻辑实体是描述业务对象某方面业务特征的一组密切相关属性的集合；<br>2.实体中的所有属性围绕同一个业务概念（业务要素），比如销售合同头，包含合同的身份标识信息、买家、卖家、交易要素、付款要素、法律要素等。<br>3.属性集合不可随意拆分；<br>4.不会出现"部分属性有值、另一部分永远为空"的情况；<br>5.业务上作为一个整体被认知和使用。 | 1.还原业务本质，切分业务对象内的构成要素，按业务特征的内聚性划分逻辑实体；<br>2.将描述同一方面特征的属性归为一个实体；<br>3.检验属性集合的业务内聚性：所有属性是否共同描述一个完整的业务概念；<br>4.检查属性填充率：同一实体的属性应基本同时有值；<br>5.避免将不同业务概念的属性强行合并到一个实体； | 1.逻辑实体属性内聚性强、围绕同一业务概念为合格；<br>2.实体内属性明显分属不同业务概念即为不合规；<br>3.实体中存在大量永远为空的属性即为不合规。 | 采购订单头中，包含了采购订单编号、供应商编码、订单日期、采购组织、货币类型、总金额、付款条款、订单状态，属性紧密依附整张采购订单头，语义统一，共同描述订单主体。 | 强行把【采购订单头 + 供应商银行信息】合并成一个逻辑实体。<br>合并属性集合：<br>采购订单编号、订单日期、总金额、供应商银行账号、开户行、银行户名 |  |
| 19 | 逻辑实体 | 识别规则 | 遵循三范式 | 解决逻辑实体设计中数据冗余、更新异常问题，通过第三范式规范确保数据一致性和减少冗余存储。 | 逻辑实体设计要遵循第三范式。在设计一个业务对象的逻辑实体时，每个逻辑实体的属性不要重复定义，不应包含其他逻辑实体中的非关键字类型的属性。 | 1.逻辑实体设计必须遵循第三范式（3NF）；<br>2.每个逻辑实体的属性不应重复定义；<br>3.实体不应包含其他逻辑实体中的非关键字属性；<br>4.非主键属性必须完全依赖于主键，不依赖于其他非主键属性；<br>5.通过外键关联而非冗余存储获取其他实体的属性。 | 1.设计逻辑实体时，检查每个非主键属性是否直接依赖于主键；<br>2.消除传递依赖：非主键属性不应依赖于另一个非主键属性；<br>3.对于其他实体的属性，通过外键关联引用，不冗余存储；<br>4.仅保留本实体自身的特征属性；<br>5.定期进行范式合规性检查。 | 1.逻辑实体符合第三范式要求为合格；<br>2.实体中冗余存储了其他实体的非关键字属性即为不合规；<br>3.存在传递依赖（非主键属性依赖另一个非主键属性）即为不合规。 | 采购订单头中，只包含供应商编码，不冗余包含供应商名称。 | 采购订单头中，只包含供应商编码，冗余包含供应商名称。 |  |
| 20 | 逻辑实体 | 识别规则 | 剔除技术数据和衍生数据 | 解决逻辑模型中混入技术表、中间表导致模型业务语义不纯的问题，确保逻辑实体层只保留具有业务语义的核心实体。 | 1、剔除技术数据（作业配置表、元数据表、技术参数配置表、接口表、映射转换表等）<br>2、剔除衍生数据（历史表、中间表、临时表、备份表、统计报表等）、<br>3、剔除日志数据（如用户操作日志、系统日志、校验日志等） | 1.剔除技术数据：作业配置表、元数据表、技术参数配置表、接口表、映射转换表等；<br>2.剔除衍生数据：历史表、临时表、备份表、统计报表等；<br>3.剔除日志数据：用户操作日志、系统日志、校验日志等；<br>4.逻辑实体应具有独立的业务语义，而非技术实现的副产品； | 1.识别候选实体时，判断其是否具有独立业务语义；<br>2.排除纯技术支撑类的表/实体（接口、配置、日志等）；<br>3.排除数据加工过程中产生的中间实体；<br>4.排除为性能或存储目的而产生的衍生实体； | 1.逻辑实体均具有独立业务语义、无技术/衍生数据为合格；<br>2.逻辑模型中包含接口表、配置表等技术数据实体即为不合规；<br>3.逻辑模型中包含临时表、历史表等衍生数据实体即为不合规。 | 比如采购需求发布任务表、采购结算单接口表、采购订单接口头表等技术数据，属于技术数据，采购需求行宽表术语衍生数据，一般不识别为逻辑实体 | 采购需求发布任务、采购订单接口头表、采购需求行宽表 |  |
| 21 | 逻辑实体 | 定义规则 | 关系实体归属原则 | 解决关系实体（关联实体）归属不清、重复定义问题，确保关系实体有明确的归属业务对象，避免数据管理职责混乱。 | 【先后顺序】：建立关系的实体在业务上如有产生的先后顺序，归属到后产生的实体所属业务对象<br>【业务主责】：判断业务的主责管理方，放置在主责管理的一方 | 1.关系实体必须归属于唯一的业务对象；<br>2.【先后顺序原则】：建立关系的实体在业务上如有产生先后顺序，归属到后产生的实体所属业务对象；<br>3.【业务主责原则】：判断业务的主责管理方，放置在主责管理的一方；<br>4.关系实体不得同时归属于多个业务对象。 | 1.识别关系实体关联的两个业务对象；<br>2.判断两个实体在业务产生上的先后顺序，将关系实体归属到后产生的一方；<br>3.判断业务主责管理方，将关系实体归属到主责管理的业务对象；<br>4.两个原则冲突时，以业务主责原则优先；<br>5.建立关系实体归属登记，确保不重复定义。 | 1.每个关系实体都有明确且唯一的归属业务对象为合格；<br>2.关系实体同时归属于多个业务对象即为不合规；<br>3.关系实体归属不符合先后顺序或业务主责原则即为不合规。 | 采购需求关系、采购需求行关系归属于业务对象“采购需求” | 逻辑实体“采购订单关系”不能归属于业务对象“采购需求”，正确的是归属于业务对象“采购订单” |  |
| 22 | 逻辑实体 | 命名规则 | 名称唯一 | 解决逻辑实体命名重复导致的同名异义、模型混乱问题，确保每个逻辑实体在数据模型中具有唯一的名称。 | 实体命名在数据模型中具有唯一性，同名必统一 | 1.逻辑实体名称在整个数据模型中必须唯一；<br>2.同名必统一：相同名称的逻辑实体必须是同一事物；<br>3.不同业务对象下的逻辑实体也不得重名；<br>4.名称变更需进行全局唯一性校验；<br>5.建立统一的命名空间管理。 | 1.建立逻辑实体命名全局登记机制；<br>2.新增实体前进行全库名称检索校验；<br>3.对简称、别名也进行唯一性校验；<br>4.当不同业务对象出现同名实体时，判断是否为同一事物，是则合并复用，否则分别命名；<br>5.建立命名评审流程。 | 1.全量逻辑实体名称无重复为合格；<br>2.存在同名但含义不同的逻辑实体即为不合规；<br>3.存在同一事物多种名称即为不合规。 | 比如逻辑实体“采购需求头”，企业内只有一个 | 存在多个重复的逻辑实体“采购需求头” |  |
| 23 | 逻辑实体 | 命名规则 | 名词命名 | 解决逻辑实体命名使用动词或动宾短语导致的语义不清问题，确保逻辑实体名称使用规范的名词形式。 | 逻辑实体名称原则上必须是名词 | 1.逻辑实体名称原则上必须是名词；<br>2.不使用动词或动宾短语命名；<br>3.名称应表达"是什么"而非"做什么"；<br>4.避免使用动作性词汇。 | 1.使用名词形式命名逻辑实体；<br>2.将动词性名称改写为名词形式（如"采购申请"改为"采购申请单"或确认其名词属性）；<br>3.检查名称是否表达实体的本质而非操作；<br>4.与业务术语库对齐，使用规范的名词术语。 | 1.逻辑实体名称均为名词形式为合格；<br>2.名称中包含动词或为动宾结构即为不合规。 | 采购需求头 | 采购申请 |  |
| 24 | 逻辑实体 | 命名规则 | 避免虚词 | 解决逻辑实体名称冗余、不规范问题，通过去除虚词和不规范字符确保名称简洁规范。 | 原则上不使用介词/连词之类的虚词 ：在，被，的等均应去除，中文名称中尽量避免使用英文，禁止使用符号，开始和末尾原则上禁止使用数字 | 1.原则上不使用介词、连词等虚词（在、被、的等均应去除）；<br>2.中文名称中尽量避免使用英文；<br>3.禁止使用符号；<br>4.开始和末尾原则上禁止使用数字；<br>5.名称应简洁、规范、无冗余。 | 1.去除名称中的虚词、助词、介词等冗余成分；<br>2.优先使用纯中文命名，确需英文时使用规范术语且尽量少用；<br>3.名称中不包含特殊符号、标点、空格等；<br>4.不以数字开头或结尾（除非有明确业务含义且符合规范）；<br>5.建立命名审核机制，检查虚词和特殊字符。 | 1.逻辑实体名称无虚词、无符号、不以数字开头/结尾为合格；<br>2.名称中包含"的""在""被"等虚词即为不合规；<br>3.名称中包含符号、以数字开头或结尾即为不合规。 | 采购需求头 | 采购需求头1 |  |
| 25 | 逻辑实体 | 命名规则 | 符合行规 | 解决逻辑实体命名与行业术语脱节、用字不规范导致的沟通障碍问题，确保命名符合行业通用习惯和用字规范。 | 名称能够完整准确表述业务含义，符合企行业内、行业内的通用习惯和规范 | 1.名称能够完整准确表述业务含义；<br>2.符合企业内部的通用习惯和规范；<br>3.符合所在行业的通用习惯和规范；<br>4.用字规范，符合国家或行业标准用字要求；<br>5.避免错别字、异体字、不规范用字。 | 1.参考行业标准、数据模型标准中的实体命名；<br>2.与企业业务术语库对齐；<br>3.注意行业特定用字规范（如财务领域"账"与"帐"的区别）；<br>4.组织业务专家和文字专家评审命名；<br>5.建立术语用字规范表。 | 1.逻辑实体命名符合行业通用习惯和用字规范为合格；<br>2.使用不规范用字或错别字即为不合规；<br>3.命名与行业通用术语差异过大且无充分理由即为不合规。 | 供应商账户 | 供应商帐户（财政部有要求，与财经相关的用“账”字、不用“帐”）。 |  |
| 26 | 逻辑实体 | 命名规则 | 关系实体命名规范 | 解决关系实体命名不规范、无法从名称判断关联对象和归属的问题，确保关系实体命名具有统一的模式和可识别性。 | 关系实体命名规范为“实体1和实体2关系”，关系实体关联的实体1和实体2必须存在，其中根据关系实体归属原则判断后产生的实体（如实体2）在名称中放在后方 | 1.关系实体命名规范为"实体1和实体2关系"的格式；<br>2.关系实体关联的实体1和实体2必须存在；<br>3.根据关系实体归属原则，后产生的实体（如实体2）在名称中放在后方；<br>4.名称中应体现关系的性质（如分摊、关联、映射等）；<br>5.不得省略关联实体名称。 | 1.识别关系实体关联的两个实体；<br>2.按"先产生的实体+和+后产生的实体+关系性质+关系"的模式命名；<br>3.确保两个实体名称完整出现在关系实体名称中；<br>4.验证关系实体的归属与命名中的位置对应（后产生的在后面，归属于后面的实体所属业务对象）；<br>5.避免使用省略式命名（如只写一方+关系）。 | 1.关系实体命名符合"实体1和实体2关系"规范为合格；<br>2.关系实体名称中缺少关联实体即为不合规；<br>3.实体顺序与归属原则不一致即为不合规。 | 采购需求和采购订单分摊关系 | 采购订单分摊关系 |  |
| 27 | 逻辑实体 | 命名规则 | 剔除特定关键字 | 解决逻辑实体命名带有物理表特征、混淆逻辑模型与物理模型边界的问题，确保逻辑实体命名体现业务语义而非物理实现。 | 实体命名中不能带“表、文件、菜单和报告”等关键字，（特定业务场景的除外，比如财务领域的“资产负债表”、“利润表”，可以使用） | 1.实体命名中不能带"表"字；<br>2.实体命名中不能带"文件"字；<br>3.实体命名中不能带"菜单"字；<br>4.实体命名中不能带"报告"字；<br>5.特定业务场景除外（如财务领域的"资产负债表""利润表"等业务概念本身含"表"字）。 | 1.逻辑实体命名从业务语义出发，不使用物理实现相关词汇；<br>2.去除名称中的"表""文件""菜单""报告"等技术/物理特征词；<br>3.对于业务概念本身包含这些字的（如财务报表），需经过评审确认其业务本质；<br>4.建立命名关键字检查清单；<br>5.定期进行命名规范巡检。 | 1.逻辑实体名称不含"表/文件/菜单/报告"等关键字为合格（特定业务场景除外）；<br>2.非特定场景下名称包含上述关键字即为不合规；<br>3.特定场景未经评审确认即为不合规。 | 采购需求头 | 采购需求头表 |  |
| 28 | 逻辑实体 | 定义规则 | 编码唯一 | 解决逻辑实体编码不统一、各领域编码规则不一致问题，确保编码全局唯一且遵循统一规范。 | 逻辑实体元素的编码，在企业内唯一，并且遵循相同的编码规范 | 1.逻辑实体元素的编码在企业内必须唯一；<br>2.所有逻辑实体遵循相同的编码规范；<br>3.编码应包含可识别的分类信息（如领域前缀、实体类型前缀）；<br>4.编码应有统一的长度和格式规范；<br>5.编码变更需遵循统一的变更流程。 | 1.制定企业统一的逻辑实体编码规范（如LE+领域缩写+序列号）；<br>2.为每个逻辑实体分配符合规范的唯一编码；<br>3.建立编码分配登记台账，防止重码；<br>4.编码中体现业务领域、实体类型等分类信息；<br>5.定期进行编码合规性巡检。 | 1.所有逻辑实体编码全局唯一且遵循统一规范为合格；<br>2.存在重复编码即为不合规；<br>3.不同领域编码规则不统一即为不合规。 | LEPUR0001（编码规范由企业自行设计，此处只是给示例LE+领域缩写+4位序列号）） | 不同的业务领域，编码规范不一致，有是“LE”开头，有的是序列号，有的是“LJST”开头，五花八门 |  |
| 29 | 逻辑实体 | 定义规则 | 主逻辑实体唯一 | 解决业务对象内核心实体不明确、多个实体平级导致模型结构混乱问题，确保每个业务对象有且仅有一个主逻辑实体作为核心。 | 主逻辑实体表述了业务对象的主要业务关注，每个业务对象有且只有一个主逻辑实体，其他实体都是围绕主逻辑实体建立关联关系。业务对象内，主逻辑实体建立之后，其他实体才能被建立；同理，一般而言，其他实体消亡后，主逻辑实体最后才可消亡。 | 1.每个业务对象有且只有一个主逻辑实体；<br>2.主逻辑实体表述业务对象的主要业务关注；<br>3.其他实体都是围绕主逻辑实体建立关联关系；<br>4.业务对象内，主逻辑实体建立之后，其他实体才能被建立；<br>5.一般而言，其他实体消亡后，主逻辑实体最后才可消亡。 | 1.为每个业务对象确定一个主逻辑实体（通常是"头"实体或核心实体）；<br>2.主逻辑实体应能代表业务对象的核心业务语义；<br>3.梳理其他实体与主逻辑实体的关联关系（直接或间接）；<br>4.验证实体创建依赖顺序：主实体先建，其他实体后建；<br>5.验证实体消亡顺序：其他实体先消亡，主实体最后消亡。 | 1.每个业务对象有且仅有一个主逻辑实体为合格；<br>2.业务对象无主逻辑实体即为不合规；<br>3.业务对象有多个主逻辑实体即为不合规。 | 业务对象“采购需求”中包含一个“采购需求头”主逻辑实体 | 业务对象“采购需求”中包含“采购需求头”、“采购需求行”两个主逻辑实体 |  |
| 30 | 逻辑实体 | 定义规则 | 必须有主键 | 解决逻辑实体无唯一标识、无法唯一区分实例导致的数据操作和关联困难问题，确保每个逻辑实体都有业务主键。 | 实体应该具有业务主键，用于唯一标识一个逻辑实体内的数据 | 1.每个逻辑实体必须具有业务主键；<br>2.主键用于唯一标识一个逻辑实体内的数据；<br>3.主键值不能为空；<br>4.主键值必须唯一；<br>5.主键应具有业务含义而非纯技术流水号。 | 1.为每个逻辑实体定义业务主键；<br>2.选择具有业务含义、能唯一标识实例的属性作为主键；<br>3.验证主键的唯一性和非空性；<br>4.主键可以是单属性或组合属性；<br>5.确保主键与业务认知一致。 | 1.每个逻辑实体都有明确定义的业务主键为合格；<br>2.实体无主键即为不合规；<br>3.主键不能唯一标识实例即为不合规。 | 采购需求头的主键是“采购需求编号” | 没有设置主键 |  |
| 31 | 逻辑实体 | 定义规则 | 主键稳定 | 解决主键值频繁变更导致的关联混乱、历史数据追溯困难问题，确保主键在实体生命周期内保持稳定不变。 | 标识符的取值在其生命周期过程中，是不应该变化或废止的，这点适用于该实体的所有取值样本 | 1.主键的取值在其生命周期过程中不应变化或废止；<br>2.这点适用于该实体的所有取值样本；<br>3.主键一经产生即不可修改；<br>4.避免使用会发生变更的业务属性作为主键。 | 1.选择稳定不变的业务属性作为主键；<br>2.避免使用名称、状态等可变属性作为主键；<br>3.设计主键生成规则，确保一经生成不再变更；<br>4.验证历史数据中主键值的稳定性；<br>5.对于可能变更的业务标识，使用独立的编码主键而非业务名称。 | 1.主键值在生命周期内稳定不变为合格；<br>2.使用可变属性（如名称）作为主键即为不合规；<br>3.存在主键值变更的情况即为不合规。 | 采购需求编号一经产生，就不会被修改 | 用采购需求名称做主键 |  |
| 32 | 逻辑实体 | 定义规则 | 主键有业务含义 | 解决主键使用无意义流水号导致的可读性差、人工无法识别问题，确保主键具有业务含义，便于理解和使用。 | 主键是有业务含义的，对所有用户而言均可获取、理解和使用，即实体的所有创造者和使用者都可以获取 | 1.主键必须有业务含义；<br>2.对所有用户而言均可获取、理解和使用；<br>3.实体的所有创造者和使用者都可以获取主键含义；<br>4.主键中可编码业务信息（如组织、日期、类型等）；<br>5.避免使用纯系统生成的无意义流水号作为主键。 | 1.设计具有业务含义的主键编码规则；<br>2.主键中可包含组织编码、日期、类型、序列号等可识别信息；<br>3.确保业务人员能通过主键大致理解其含义；<br>4.避免使用纯数字流水号或UUID作为业务主键；<br>5.与业务人员确认主键编码规则的可理解性。 | 1.主键具有明确业务含义、业务人员可理解为合格；<br>2.使用纯系统流水号且无业务含义作为主键即为不合规；<br>3.业务人员无法从主键获取任何识别信息即为不合规。 | 比如采购需求编号280020260604000004，标识管理单元2800下在2026年6月4日创建的第4单需求 | 采购需求ID为1968161303673506831，只是系统生成的唯一流水号，不具备可识别性 |  |
| 33 | 逻辑实体 | 定义规则 | 实体归属唯一 | 解决逻辑实体同时归属于多个业务对象导致的管理职责不清、重复建设问题，确保每个逻辑实体有唯一的归属业务对象。 | 逻辑实体必须归属于唯一的业务对象 | 1.每个逻辑实体必须归属于唯一的业务对象；<br>2.逻辑实体不得同时归属于多个业务对象；<br>3.归属应基于业务主责原则确定；<br>4.其他业务对象通过关联关系引用该实体，而非重复归属。 | 1.为每个逻辑实体确定唯一的归属业务对象；<br>2.判断业务主责管理方，实体归属于主责方的业务对象；<br>3.对于跨领域使用的实体，确定主责业务对象，其他领域通过关联引用；<br>4.建立实体归属登记台账；<br>5.定期检查实体归属的唯一性。 | 1.每个逻辑实体归属于唯一的业务对象为合格；<br>2.实体同时归属于多个业务对象即为不合规；<br>3.实体无明确归属业务对象即为不合规。 | 采购需求关系、采购需求行关系归属于业务对象“采购需求” | 逻辑实体“采购订单关系”即归属于业务对象“采购需求”，又归属于“采购订单”，是错误的。正确答案的是归属于业务对象“采购订单” |  |
| 34 | 逻辑实体 | 定义规则 | 描述内容完整 | 解决逻辑实体定义不清、描述不完整导致的理解不一致问题，确保每个逻辑实体都有完整清晰的定义说明。 | 逻辑实体应该有明确的描述，包括目的（为什么）、定义（是什么）和范围（含哪些），其中业务对象范围不应局限于某类产品<br>目的解释了为什么这个实体对于业务是重要的，回答了“为何对业务而言是重要的”这个问题；如产品合约生命周期目的：为了反映产品合约生命周期的变化过程，提升内部管理能力及客户满意度。<br>定义解释了实体的含义，回答了“是什么”；如产品合约生命周期定义：描述产品协议生命周期的变化过程和审批情况的信息。<br>定义解释了实体的含义，回答了“是什么”；如产品合约生命周期定义：描述产品协议生命周期的变化过程和审批情况的信息。 | 1.逻辑实体必须有明确的描述；<br>2.描述必须包含目的（为什么）：解释该实体为何对业务重要；<br>3.描述必须包含定义（是什么）：清晰说明实体的本质含义；<br>4.描述应包含主要属性的说明；<br>5.描述应具有业务语义，而非技术描述，慎用“记录XXX”这种技术语言描述。 | 1.按"目的-定义"结构编写逻辑实体描述；<br>2.目的部分回答"为什么这个实体对业务重要"；<br>3.定义部分回答"这个实体是什么，描述什么业务信息"；<br>4.可列举主要属性以辅助理解；<br>5.确保描述从业务视角出发，而非物理表结构描述；<br>6.组织业务专家评审描述的完整性和准确性。 | 1.逻辑实体描述包含目的和定义，具有完整业务语义为合格；<br>2.描述过于肤浅、偏物理表设计即为不合规；<br>3.描述缺少目的或定义即为不合规。 | 采购需求头：是指一份采购需求申请单的摘要和控制信息，用于汇总一份完整的物资或服务申请，并驱动后续的审批、寻源和采购流程。主要属性包括：需求单号、申请人、申请部门、需求日期、需求状态、采购类型等。 | 采购需求头：是记录采购需求部门提交的申请信息，太肤浅，太偏物理表设计，没有准确的表达式什么。 |  |
| 35 | 业务属性 | 识别规则 | 原子性 | 解决业务属性粒度过粗、包含多个语义导致的使用困难和数据冗余问题，确保每个属性是不可再拆分的最小业务语义单元。 | 最小业务语义单元，不可再拆分 | 1.业务属性必须是最小业务语义单元；<br>2.属性不可再拆分为多个有独立意义的子属性；<br>3.每个属性只表达一个业务含义；<br>4.避免将多个概念组合在一个属性中；<br>5.每个业务属性的一条记录内，不能出现多值。 | 1.识别业务属性时，检查其是否可进一步拆分为更小的语义单元；<br>2.如果一个属性包含多个独立业务含义，应拆分为多个属性；<br>3.确保每个属性只回答一个"是什么"的问题；<br>4.区分属性与实体：如果一个概念还能拆出多个属性，它可能是实体而非属性，比如客户联系电话，如果有多个联系电话，需要拆分、单独设计为一个逻辑实体 | 1.业务属性均为不可再分的最小语义单元为合格；<br>2.属性可拆分为多个独立语义即为不合规；<br>3.一个属性表达多个业务含义即为不合规。 | 采购需求行号 | 采购需求行 |  |
| 36 | 业务属性 | 识别规则 | 必要性 | 解决业务属性冗余、无用属性堆积导致的模型臃肿和维护成本高问题，确保每个属性都有明确的业务用途。 | 识别的每一个业务属性，都有明确的含义和用途，满足业务需求 | 1.识别的每一个业务属性都必须有明确的含义和用途；<br>2.属性必须满足业务需求；<br>3.不为不存在的业务场景预留属性；<br>4.可通过计算或推导获得的属性一般不冗余存储（视具体场景而定）。 | 1.为每个业务属性明确其业务用途和使用场景；<br>2.删除无明确用途的属性；<br>3.验证属性是否为业务流程或数据分析所必需；<br>4.避免"以防万一"式的属性堆砌；<br>5.对于可推导属性，评估是否需要冗余存储（性能与一致性的权衡）。 | 1.每个业务属性都有明确的业务用途为合格；<br>2.存在无明确用途的"摆设"属性即为不合规；<br>3.属性与所在实体的业务场景不匹配即为不合规。 | 采购需求行中包含“需求日期” | 采购需求行中包含“发货日期”<br>、采购订单中未包含“采购金额”，采购金额有抹零、一口价等场景，无法通过采购数量X采购单价加总获得 |  |
| 37 | 业务属性 | 识别规则 | 剔除技术字段 | 解决业务逻辑模型中混入技术字段导致的业务语义不纯问题，确保业务属性层只包含具有业务含义的属性，技术字段在物理模型阶段考虑。 | 无业务含义的技术字段不作为属性纳入业务逻辑模型中（在物理模型阶段再考虑）； | 1.无业务含义的技术字段不作为属性纳入业务逻辑模型；<br>2.业务属性应反映业务信息，而非系统技术信息；<br>3.需剔除的典型技术字段包括：租户ID、删除标记、创建人、最后修改人、最后修改日期、最后修改跟踪ID、最后修改版本等。 | 1.梳理候选属性列表（如数据库表字段），区分业务属性与技术字段；<br>2.将纯技术支撑类字段排除在业务逻辑模型之外；<br>3.剔除扩展属性和审计字段（如剔除租户ID、删除标记、创建人、最后修改人、最后修改日期、最后修改跟踪ID、最后修改版本等）<br>4、除上述字段/属性外，字段名称末尾以“ID”结尾的，一律修改“ID”为“编码”； | 1.业务逻辑模型中无技术字段为合格；<br>2.逻辑模型中包含租户ID、删除标记等技术字段即为不合规；<br>3.将系统审计字段纳入业务属性即为不合规。 | 剔除租户ID、删除标记、创建人、最后修改人、最后修改日期、最后修改跟踪ID、最后修改版本 | 没有剔除租户ID、删除标记、创建人、最后修改人、最后修改日期、最后修改跟踪ID、最后修改版本 |  |
| 38 | 业务属性 | 命名规则 | 业务词汇 | 解决业务属性命名口语化、不规范导致的理解不一致问题，确保属性命名使用正式的业务词汇。 | 采用业务词汇 | 1.业务属性命名必须采用业务词汇；<br>2.使用正式、规范的业务术语，而非口语化表达；<br>3.命名应具有通用性，不局限于特定场景的俗称；<br>4.与企业业务术语库保持一致。 | 1.参考企业业务术语库、数据标准中的属性命名；<br>2.将口语化名称替换为正式业务术语；<br>3.避免使用过于狭窄场景的俗称；<br>4.组织业务专家评审命名的规范性；<br>5.建立属性命名术语对照表。 | 1.业务属性命名使用规范业务词汇为合格；<br>2.使用口语化、俗称化命名即为不合规；<br>3.命名与业务术语库不一致即为不合规。 | 物料编码 | 原料编码（太过于口语化，且场景狭窄，不是正式业务词汇） |  |
| 39 | 业务属性 | 命名规则 | 名称贯标 | 解决同一业务概念多种命名、同名不同义导致的数据标准不统一问题，确保属性命名遵循企业级数据标准。 | 属性应有明确的业务含义和规则，企业级唯一且共享（遵循数据标准），不能有同义不同名、或同名不同义情况，业务属性的命名规范要尽量和数据标准的数据项定义保持一致 | 1.属性应有明确的业务含义和规则；<br>2.属性命名企业级唯一且共享；<br>3.遵循数据标准，不能有同义不同名、同名不同义情况；<br>4.业务属性命名规范尽量与数据标准的数据项定义保持一致；<br>5.跨系统、跨领域的同一属性使用相同名称。 | 1.建立企业级数据标准（数据项定义）作为命名依据；<br>2.业务属性命名与数据标准中的数据项对齐；<br>3.新增属性前检索数据标准，已有标准的直接复用；<br>4.建立同义不同名、同名不同义的识别和治理机制；<br>5.定期进行属性命名与数据标准的一致性检查。 | 1.业务属性命名与数据标准一致、企业级唯一为合格；<br>2.存在同义不同名即为不合规；<br>3.存在同名不同义即为不合规。 | 采购需求编号，属于数据标准定义的内容 | 采购需求号码，不是数据标准定义的内容 |  |
| 40 | 业务属性 | 命名规则 | 顾名思义 | 解决业务属性命名晦涩、难以理解导致的使用困难和误用问题，确保属性名称清晰易懂、见名知义。 | 属性的名称应该是清晰、见名知义、易于理解的 | 1.属性的名称应该是清晰的；<br>2.属性名称应见名知义、易于理解；<br>3.名称应直接表达属性的业务含义；<br>4.避免使用缩写、简称导致的理解困难。 | 1.使用清晰、直白的词汇命名属性；<br>2.确保业务人员看到名称就能理解属性含义；<br>3.避免使用过于专业或生僻的术语；<br>4.对于容易产生歧义的名称，增加定义说明或调整命名；<br>5.组织业务人员进行名称可理解性评审。 | 1.业务属性名称清晰、见名知义为合格；<br>2.名称晦涩、业务人员无法直接理解即为不合规；<br>3.名称容易产生歧义即为不合规。 | 采购单价 | 请购单价 |  |
| 41 | 业务属性 | 命名规则 | 词汇简练 | 解决业务属性命名过长、冗余导致的使用不便问题，确保属性名称在保证含义清晰的前提下尽可能简练。 | 用尽可能简练的词汇唯一标识属性并表达含义 | 1.用尽可能简练的词汇唯一标识属性；<br>2.名称应表达含义，但不过度冗余；<br>3.去除不必要的修饰词和重复信息；<br>4.在不产生歧义的前提下尽量简短。 | 1.在保证含义清晰的前提下，尽量精简属性名称；<br>2.去除冗余的修饰词、限定词（上下文中已明确的信息无需重复）；<br>3.避免将实体名称重复嵌入属性名称；<br>4.平衡简练性与可理解性，不因过度精简导致歧义；<br>5.建立命名长度参考标准。 | 1.业务属性名称简练且无歧义为合格；<br>2.名称过长、包含冗余信息即为不合规；<br>3.因过度精简导致含义不清即为不合规。 | 自动创建标识 | 采购需求自动创建标识 |  |
| 42 | 业务属性 | 命名规则 | 少用特殊字符 | 解决属性命名使用特殊字符导致的程序冲突、SQL语法错误问题，确保属性命名规范、避免技术冲突。 | 尽可能少的使用程序和SQL中的一些关键词、算式运算相关的符号等特殊字符，避免引起冲突。 | 1.尽可能少使用程序和SQL中的关键词；<br>2.尽可能少使用算式运算相关的符号等特殊字符；<br>3.避免使用&、-、+、/、*等特殊符号；<br>4.避免使用数据库保留关键字；<br>5.特殊字符可能引起冲突，应尽量规避。 | 1.建立属性命名禁用字符清单；<br>2.避免使用SQL保留字和程序关键字作为属性名；<br>3.避免使用运算符号和特殊符号；<br>4.使用中文或英文规范词汇命名，不用符号代替；<br>5.属性命名后进行关键字和特殊字符检查。 | 1.业务属性命名无特殊字符、无SQL关键字为合格；<br>2.名称包含&、-、+、/、*等特殊符号即为不合规；<br>3.使用SQL或程序保留关键字即为不合规。 |  | 名称包含“&”、“-”、“+”、“/”、“*”等 |  |
| 43 | 概念模型图 | 识别规则 | 避免孤岛 | 解决概念模型中业务对象孤立存在、无法体现业务关联的问题，确保概念模型图中业务对象之间形成完整的关系网络。 | 业务对象关系识别时，原则上概念模型图中业务对象不应出现孤岛，都应与其他业务对象存在关系 | 1.概念模型图中业务对象原则上不应出现孤岛；<br>2.每个业务对象都应与其他业务对象存在关系；<br>3.孤岛对象意味着其在业务场景中与其他对象无关联，通常是识别错误或遗漏关系；<br>4.关系应反映真实的业务联系。 | 1.绘制概念模型图时，检查每个业务对象是否有关联的其他业务对象；<br>2.对于孤岛对象，分析其业务场景，找出应有的关联关系；<br>3.补充缺失的业务对象关系；<br>4.验证关系是否反映真实的业务逻辑； | 1.概念模型图中无孤岛业务对象为合格；<br>2.存在无任何关联的孤岛业务对象即为不合规；<br>3.孤岛对象经分析确有业务关联但未建立关系即为不合规。 |  | 采购管理概念模型图中，“采购需求”没有与其他业务对象建立关系，正常情况下需要与供应商、采购订单建立关系 |  |
| 44 | 概念模型图 | 识别规则 | 关系基数 | 解决概念模型中关系基数未定义导致的业务语义不完整、下游逻辑模型设计缺乏依据问题，确保业务对象之间的关系有明确的基数定义。 | 概念模型图中，业务对象之间的连线定义时，需要定义关系基数 | 1.概念模型图中业务对象之间的连线必须定义关系基数；<br>2.关系基数应准确反映业务规则（1:1、1:N、M:N等）；<br>3.关系基数应从两个方向分别定义；<br>4.关系基数定义应与业务实际一致。 | 1.为概念模型中每条关系线定义两端的关系基数；<br>2.从业务规则出发，确定一个对象实例对应另一个对象的实例数量；<br>3.两个方向都要定义基数（如A对B是1:N，B对A是1:1）；<br>4.与业务专家确认关系基数的准确性；<br>5.使用标准的基数表示法（如乌鸦脚表示法）。 | 1.概念模型中所有关系都有明确的基数定义为合格；<br>2.存在未定义基数的关系即为不合规；<br>3.关系基数与业务实际不符即为不合规。 |  |  |  |
| 45 | 逻辑模型图 | 定义规则 | 与概念模型保持一致 | 解决逻辑模型与概念模型脱节、逻辑关系违背业务对象关系的问题，确保逻辑模型忠实于概念模型的业务语义。 | 逻辑模型需要遵从概念模型，逻辑实体之间的关系必须遵从业务对象之间的关系，保持逻辑一致。 | 1.逻辑模型必须遵从概念模型；<br>2.逻辑实体之间的关系必须遵从业务对象之间的关系；<br>3.保持逻辑一致，不得出现概念模型中不存在的关系；<br>4.概念模型中的关系在逻辑模型中应有对应的实体关系体现。 | 1.设计逻辑模型时，以概念模型为业务依据；<br>2.检查逻辑实体关系是否与对应业务对象关系一致；<br>3.概念模型中没有关系的业务对象，其逻辑实体之间也不应建立关系；<br>4.概念模型中有关系的，逻辑模型中应有对应的实体关系实现；<br>5.定期进行逻辑模型与概念模型的一致性校验。 | 1.逻辑模型与概念模型关系一致为合格；<br>2.逻辑模型中出现概念模型不存在的关系即为不合规；<br>3.概念模型中存在的关系在逻辑模型中未体现即为不合规。 |  | 概念模型中，采购订单和客户没有关系，但在逻辑模型图中，客户和采购订单头1:N关系 |  |
| 46 | 逻辑模型图 | 识别规则 | 避免孤岛 | 解决逻辑模型中实体孤立存在、关系不完整问题，确保逻辑实体之间形成完整的关联网络。 | 逻辑实体关系识别时，原则逻辑模型图中逻辑实体不应出现孤岛，都应与其他业务对象存在关系 | 1.逻辑模型图中逻辑实体原则上不应出现孤岛；<br>2.每个逻辑实体都应与其他逻辑实体存在关系；<br>3.孤岛实体可能意味着关系识别遗漏或实体识别错误；<br>4.同一业务对象内的实体应有主从关联关系。 | 1.绘制逻辑模型图时，检查每个逻辑实体是否有关联的其他实体；<br>2.对于孤岛实体，分析其业务场景，找出应有的关联关系；<br>3.同一业务对象内的实体应与主逻辑实体建立关联；<br>4.跨业务对象的实体关系应与概念模型关系对应；<br>5.补充缺失的实体关系，消除孤岛。 | 1.逻辑模型图中无孤岛逻辑实体为合格；<br>2.存在无任何关联的孤岛逻辑实体即为不合规；<br>3.同一业务对象内的实体未与主实体关联即为不合规。 |  | 采购管理逻辑模型图中，“采购订单发运行”没有与其他逻辑实体建立关系，正常情况下需要与采购订单行、采购订单分摊行等建立关系 |  |
| 47 | 逻辑模型图 | 定义规则 | 关系基数 | 解决逻辑模型中实体关系基数未定义导致的设计不精确问题，确保逻辑实体之间的关系有明确的基数约束。 | 逻辑模型图中，逻辑之间的连线定义时，需要定义关系基数 | 1.逻辑模型图中逻辑实体之间的连线必须定义关系基数；<br>2.关系基数应准确反映业务规则；<br>3.关系基数应从两个方向分别定义（父对子、子对父）；<br>4.基数定义应与业务实际和主键外键关系一致。 | 1.为逻辑模型中每条关系线定义两端的关系基数；<br>2.基于业务规则和主键外键约束确定基数；<br>3.两个方向都要明确定义（如1:0..N、1:1等）；<br>4.验证基数定义与外键约束的一致性；<br>5.与业务专家确认关系基数的业务准确性。 | 1.逻辑模型中所有关系都有明确的基数定义为合格；<br>2.存在未定义基数的关系即为不合规；<br>3.关系基数与外键约束或业务规则不一致即为不合规。 |  |  |  |
| 48 | 逻辑模型图 | 定义规则 | 主外键依赖关系 | 解决主外键关系定义不规范、一个外键同时依赖多个主键导致的关系混乱问题，确保主外键依赖关系清晰明确。 | 两个逻辑实体之间建立主外键关系，父实体必须有主键，子实体的外键依赖于父实体的主键，且子实体的一个外键仅依赖于一个父实体的主键 | 1.两个逻辑实体之间建立主外键关系时，父实体必须有主键；<br>2.子实体的外键依赖于父实体的主键；<br>3.子实体的一个外键仅依赖于一个父实体的主键；<br>4.一个外键不得同时与多个父实体的主键建立依赖关系；<br>5.主外键关系应明确、无歧义。 | 1.建立主外键关系前，确认父实体有明确定义的主键；<br>2.子实体的外键属性应与父实体主键类型一致；<br>3.每个外键只能引用一个父实体的主键；<br>4.如果需要关联多个父实体，应使用多个外键字段分别关联；<br>5.绘制主外键关系图，确保依赖关系清晰。 | 1.所有主外键关系符合规范（父有主键、外键仅依赖一个主键）为合格；<br>2.一个外键同时依赖多个父实体主键即为不合规；<br>3.父实体无主键但建立了主外键关系即为不合规。 |  | 采购订单中的客户编号作为外键同时与客户、员工的主键建立外键关系 |  |
| 49 | 逻辑模型图 | 定义规则 | 禁止M:N关系 | 解决逻辑模型中保留多对多关系导致的物理实现困难、数据冗余问题，确保逻辑模型中所有关系都可直接物理实现。 | 禁止逻辑模型保留 M:N 关系，必须拆分为两个1:N + 中间关联实体(关系实体) | 1.禁止逻辑模型保留M:N（多对多）关系；<br>2.M:N关系必须拆分为两个1:N关系 + 中间关联实体（关系实体）；<br>3.中间关系实体承载两个实体的关联信息；<br>4.拆分后的关系实体应有自己的属性和主键。 | 1.识别逻辑模型中的多对多关系；<br>2.为每个M:N关系创建中间关系实体；<br>3.将M:N关系拆分为两个1:N关系，分别连接原实体与关系实体；<br>4.为关系实体定义主键和必要的属性（如关联时间、状态等）；<br>5.验证拆分后的关系是否准确反映原业务语义。 | 1.逻辑模型中无M:N关系，均已拆分为关系实体为合格；<br>2.存在未拆分的M:N关系即为不合规；<br>3.拆分后的关系实体不能准确反映原业务语义即为不合规。 | 采购需求和采购订单分摊关系 | 采购订单头和采购需求头直接建立M:N关系 |  |
